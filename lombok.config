
# Do not hunt for config files outside the project
config.stopBubbling = true

# Warn for some of the weirder annotations.
lombok.sneakyThrows.flagUsage = warning
lombok.val.flagUsage = error
lombok.var.flagUsage = error

# Autogenerated code config
lombok.log.fieldName = logger
lombok.addNullAnnotations = javax
lombok.addLombokGeneratedAnnotation = true
# Uncomment if we ever set up findbugs/spotbugs - at present this produces code which cannot compile
# lombok.extern.findbugs.addSuppressFBWarnings = true
